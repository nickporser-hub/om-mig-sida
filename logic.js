/*
setInterval(SpawnImage, 20000);
setTimeout(SpawnImage, 3000)


const images = [];

function SpawnImage()
{
    const ad = createElement("div");
    ad.classList.add("ad");

    document.body.appendChild(ad);

    images.push(ad);

    ad.addEventListener("mouseDown", (e) =>
    {
        if (e.button !== 0) return;

    }) 
}
*/



